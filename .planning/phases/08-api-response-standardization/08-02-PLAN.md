---
phase: 08-api-response-standardization
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01-PLAN.md
files_modified:
  - price_recorder_vue/src/utils/request.ts
  - price_recorder_vue/src/api/blog.ts
  - price_recorder_vue/src/api/debt.ts
  - price_recorder_vue/src/api/Login.ts
  - price_recorder_vue/src/api/Article.ts
autonomous: true
requirements:
  - API-01
  - API-05

must_haves:
  truths:
    - "Frontend Axios interceptor unwraps {code, message, data} and returns the data payload on success"
    - "Frontend Axios interceptor rejects with an Error containing the message on non-success codes"
    - "All API clients rely on the interceptor and do not manually extract res.data"
  artifacts:
    - path: "price_recorder_vue/src/utils/request.ts"
      provides: "Axios interceptor that parses the unified wrapper"
      contains: "body.code === 200"
    - path: "price_recorder_vue/src/api/blog.ts"
      provides: "Blog API client without manual res.data extraction"
      min_lines: 10
    - path: "price_recorder_vue/src/api/debt.ts"
      provides: "Debt API client without manual res.data extraction"
      min_lines: 10
    - path: "price_recorder_vue/src/api/Login.ts"
      provides: "Login API client without custom .then/.catch"
      contains: "return await instance.post(\"/user/login/v1\", data);"
    - path: "price_recorder_vue/src/api/Article.ts"
      provides: "Article API client without custom .then/.catch"
      contains: "return await instance.get(\"/post/page/v1\");"
  key_links:
    - from: "price_recorder_vue/src/utils/request.ts"
      to: "all API clients"
      via: "Axios instance import"
      pattern: "import instance from \"@/utils/request.ts\""
---

<objective>
Align the frontend to consume the new unified backend response wrapper by updating the Axios interceptor and all API clients.

Purpose: Satisfy API-05 (and API-01 from the frontend side) so that the Vue app correctly parses `{code, message, data}` and every API client relies on the interceptor for unwrapping.
Output: Updated `request.ts`, `blog.ts`, `debt.ts`, `Login.ts`, and `Article.ts` with all tests and build passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-api-response-standardization/08-CONTEXT.md
@.planning/phases/08-api-response-standardization/08-RESEARCH.md
@.planning/phases/08-api-response-standardization/08-PATTERNS.md
@price_recorder_vue/src/utils/request.ts
@price_recorder_vue/src/api/blog.ts
@price_recorder_vue/src/api/debt.ts
@price_recorder_vue/src/api/Login.ts
@price_recorder_vue/src/api/Article.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Axios response interceptor to unwrap unified wrapper</name>
  <files>price_recorder_vue/src/utils/request.ts</files>
  <read_first>
    - price_recorder_vue/src/utils/request.ts
  </read_first>
  <action>
    Replace the existing response interceptor in `price_recorder_vue/src/utils/request.ts` with the following exact code:

    ```typescript
    instance.interceptors.response.use(
      (res) => {
        const body = res.data as { code?: number; message?: string; data?: any } | undefined;
        if (!body || body.code === 200) {
          return body?.data;
        }
        return Promise.reject(new Error(body.message || "请求失败"));
      },
      (err) => {
        const msg = err.response?.data?.message || err.message || "网络错误";
        alert(msg);
        return Promise.reject(new Error(msg));
      }
    );
    ```

    Keep the request interceptor (the one that adds `Authorization: Bearer <token>`) unchanged.
  </action>
  <verify>
    <automated>
      grep -q "body.code === 200" price_recorder_vue/src/utils/request.ts && \
      grep -q "return body?.data;" price_recorder_vue/src/utils/request.ts && \
      grep -q "err.response?.data?.message" price_recorder_vue/src/utils/request.ts && \
      echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `price_recorder_vue/src/utils/request.ts` contains the literal string `body.code === 200`
    - `price_recorder_vue/src/utils/request.ts` contains the literal string `return body?.data;`
    - `price_recorder_vue/src/utils/request.ts` contains the literal string `err.response?.data?.message`
  </acceptance_criteria>
  <done>
    Axios interceptor unwraps `res.data.data` when `code === 200` and rejects with `Error(message)` on non-success codes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove manual res.data extraction from all API clients</name>
  <files>
    price_recorder_vue/src/api/blog.ts
    price_recorder_vue/src/api/debt.ts
    price_recorder_vue/src/api/Login.ts
    price_recorder_vue/src/api/Article.ts
  </files>
  <read_first>
    - price_recorder_vue/src/api/blog.ts
    - price_recorder_vue/src/api/debt.ts
    - price_recorder_vue/src/api/Login.ts
    - price_recorder_vue/src/api/Article.ts
  </read_first>
  <action>
    Update each API client file so it returns the Axios promise directly and lets the interceptor handle unwrapping.

    **price_recorder_vue/src/api/blog.ts**
    - Remove `.then((res) => res.data)` from `getPosts`, `getPostById`, `createPost`, and `updatePost`.
    - Each function should become a direct `return await instance.get(...)` or `return await instance.post(...)`.
    - `deletePost` already has no `.then`; leave it unchanged.

    **price_recorder_vue/src/api/debt.ts**
    - Remove `.then((res) => res.data)` from `getDebts`, `getDebtById`, `createDebt`, and `updateDebt`.
    - In `deleteDebt`, change `return response.data.flag;` to `return response.flag;` (the interceptor now returns the unwrapped reply directly).

    **price_recorder_vue/src/api/Login.ts**
    - Replace the entire `login` function body with exactly:
      ```typescript
      export async function login(data: userReq) {
        return await instance.post("/user/login/v1", data);
      }
      ```
    - Remove the existing `.then(res => { return res; })` and `.catch(err => { console.log(err); })` blocks.

    **price_recorder_vue/src/api/Article.ts**
    - Replace the entire `getAllArticle` function body with exactly:
      ```typescript
      export async function getAllArticle() {
        return await instance.get("/post/page/v1");
      }
      ```
    - Remove the existing `.then(res => { return res.data; })` and `.catch(err => { console.log(err); })` blocks.
  </action>
  <verify>
    <automated>
      grep -c "\.then(" price_recorder_vue/src/api/blog.ts | grep -q "^0$" && \
      grep -c "\.then(" price_recorder_vue/src/api/debt.ts | grep -q "^0$" && \
      grep -c "\.then(" price_recorder_vue/src/api/Login.ts | grep -q "^0$" && \
      grep -c "\.catch(" price_recorder_vue/src/api/Login.ts | grep -q "^0$" && \
      grep -c "\.then(" price_recorder_vue/src/api/Article.ts | grep -q "^0$" && \
      grep -c "\.catch(" price_recorder_vue/src/api/Article.ts | grep -q "^0$" && \
      grep -q "response.flag" price_recorder_vue/src/api/debt.ts && \
      echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ".then(" price_recorder_vue/src/api/blog.ts` outputs `0`
    - `grep -c ".then(" price_recorder_vue/src/api/debt.ts` outputs `0`
    - `grep -c ".then(" price_recorder_vue/src/api/Login.ts` outputs `0`
    - `grep -c ".catch(" price_recorder_vue/src/api/Login.ts` outputs `0`
    - `grep -c ".then(" price_recorder_vue/src/api/Article.ts` outputs `0`
    - `grep -c ".catch(" price_recorder_vue/src/api/Article.ts` outputs `0`
    - `price_recorder_vue/src/api/debt.ts` contains the literal string `response.flag`
  </acceptance_criteria>
  <done>
    All API clients return the Axios promise directly; no manual `res.data` extraction remains.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify frontend tests and build</name>
  <files>none</files>
  <read_first>
    - price_recorder_vue/src/utils/request.ts
    - price_recorder_vue/src/api/Login.ts
  </read_first>
  <action>
    Run the frontend test suite and production build to confirm the interceptor and API client changes work correctly.

    Execute:
    1. `cd price_recorder_vue && ./node_modules/.bin/vitest run`
    2. `cd price_recorder_vue && pnpm build`

    If either command fails, diagnose the error. Common fixes:
    - If `Login.spec.ts` fails because the mock shape changed, ensure `login()` still returns the unwrapped `LoginReply` shape (the interceptor now provides it). The existing mocks in `Login.spec.ts` already return `{ token: 'token', userId: 1 }`, which is correct.
    - If `Article.vue` causes a type error due to importing `@/api/Article.js`, this is a pre-existing issue; do not modify `Article.vue` unless it blocks the build, in which case change the import extension to `.ts` in `Article.vue`.
  </action>
  <verify>
    <automated>
      cd price_recorder_vue && ./node_modules/.bin/vitest run
    </automated>
  </verify>
  <acceptance_criteria>
    - `cd price_recorder_vue && ./node_modules/.bin/vitest run` exits with code 0
    - `cd price_recorder_vue && pnpm build` exits with code 0
  </acceptance_criteria>
  <done>
    Frontend tests and build pass with the new unified response wrapper.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Axios interceptor -> Vue components | Interceptor is the single gate that parses backend responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | Axios error handler | mitigate | Error handler reads `err.response?.data?.message` without exposing full response objects or stack traces |
</threat_model>

<verification>
- `cd price_recorder_vue && ./node_modules/.bin/vitest run` passes
- `cd price_recorder_vue && pnpm build` passes
- No `.then((res) => res.data)` patterns remain in `src/api/*.ts`
</verification>

<success_criteria>
- Axios interceptor unwraps `{code, message, data}` and returns `data` on success
- Axios interceptor rejects with `Error(message)` on non-success codes
- All API clients rely on the interceptor without manual unwrapping
- Frontend tests and production build pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-api-response-standardization/08-02-SUMMARY.md`
</output>
