---
phase: 13-fishing-spot-map
plan: 03
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - price_recorder_vue/src/api/map.ts
  - price_recorder_vue/src/stores/mapStore.ts
  - price_recorder_vue/src/view/Map.vue
  - price_recorder_vue/src/router/index.ts
  - price_recorder_vue/src/components/AppLayout.vue
  - price_recorder_vue/.env.example
  - price_recorder_vue/index.html
  - price_recorder_vue/src/__tests__/stores/mapStore.spec.ts
  - price_recorder_vue/src/__tests__/api/map.spec.ts
  - price_recorder_vue/src/__tests__/view/Map.spec.ts
  - .planning/REQUIREMENTS.md
  - .planning/phases/13-fishing-spot-map/13-VALIDATION.md
autonomous: false          # Plan-level: blocked at first task that requires Gaode env keys; inner tasks declare their own autonomous flag
requirements: [MAP-01, MAP-02, MAP-03]
tags: [vue, pinia, gaode-amap, vitest, frontend, gap-closure]

user_setup:
  - service: gaode-amap-js-api
    why: "Frontend Gaode JS map display (D-01, D-15, D-20 — AMapLoader.load needs a Web端 JS API key + paired security code)"
    env_vars:
      - name: VITE_GAODE_JS_API_KEY
        source: "Gaode开放平台控制台 console.amap.com/dev/key/app 创建「Web端(JS API)」应用取 Key"
      - name: VITE_GAODE_JS_SECURITY_CODE
        source: "同一「Web端(JS API)」应用页的「安全密钥」字段"
    dashboard_config:
      - task: "将 localhost 与生产域名加入 Key 的域名白名单"
        location: "Gaode控制台 → 我的应用 → 选中 JS API 应用 → 域名白名单"

must_haves:
  truths:
    - truth: "User can capture and save current GPS coordinates with a name/note (SC-1 — closes VERIFICATION gap #1)"
      verify: "In Map.vue, capture FAB tap → navigator.geolocation.getCurrentPosition (mockable in jsdom) → AMap.convertFrom WGS-84→GCJ-02 → mapApi.reverseGeocode called with GCJ-02 lng,lat → backend returns address → bottom-sheet opens with name input + address chip (D-17) → submit (name only required, per D-09) → mapApi.saveSpot(SpotEntity{latitude,longitude RAW WGS-84, address}) → toast '位置已保存' → marker added at GCJ-02 position. mapStore.spec.ts asserts saveSpot calls api then re-lists; map.spec.ts asserts reverseGeocode posts {latitude,longitude} to /map/reverse-geocode/v1 and saveSpot posts serialized SpotEntity WGS-84 coords to /map/save/v1."
    - truth: "User can view, edit, and delete saved spots (SC-2 — closes VERIFICATION gap #2)"
      verify: "On Map.vue mount, mapStore.listSpots() fetches /map/list/v1 → for each SpotEntity, AMap.Marker created at GCJ-02 (convertFrom WGS-84 raw). Marker click opens spot-bottom-sheet (D-10) with Edit/Delete/Navigate buttons. Edit reuses capture bottom-sheet pre-filled (D-15) → mapApi.updateSpot → toast. Delete shows confirm dialog → mapApi.deleteSpot(id) returns {success:true} → marker removed + mapStore local-mutates spots (mirror fuelStore.deleteVehicle filter). mapStore.spec.ts asserts deleteSpot filters locally; map.spec.ts asserts deleteSpot posts {id} to /map/delete/v1; MUST assert reply cast as {success:boolean} NOT {flag} (PATTERNS risk #1)."
    - truth: "User can view spots on a map and navigate back to them (SC-3 — closes VERIFICATION gap #3)"
      verify: "Full-screen AMap.Map renders (manual UAT — D-22 skip-if AMap.Map not a function in jsdom). Each saved spot has a marker. Marker tap → bottom-sheet → Navigate button → window.location.href set to androidamap://navi?sourceApplication=blog&poiname=<name>&lat=<gcj02_lat>&lon=<gcj02_lng>&dev=0&style=2 (or iosamap:// on iOS UA) with 2s setTimeout fallback to https://uri.amap.com/navigation?to=<lng>,<lat>,<name> (RESEARCH Pattern 6). Map.spec.ts asserts nav button sets window.location.href to the amap scheme and dev=0 (frontend already converted → GCJ-02)."
    - truth: "Spot data persisted in DB survives reload and pipeline runs on Vitest+jsdom (SC-4 — closes VERIFICATION gap #4)"
      verify: "saveSpot posts serialized SpotEntity (lat/lng as strings via decimalToString per fuel.ts:69-72) to /map/save/v1 → backend persists via GORM (shipped in 13-01). On reload, listSpots() returns the same SpotEntity. map.spec.ts asserts serialization. Vitest suite (pnpm test:unit) green. Mobile touch gestures (pinch-zoom, pan, pin tap) enumerated as skipped tests per D-22 and re-routed to 13-VALIDATION.md manual UAT."
  artifacts:
    - path: "price_recorder_vue/src/api/map.ts"
      provides: "Axios API client: createSpot, listSpots, getSpot(path-template), updateSpot, deleteSpot(returns {success}), reverseGeocode, uploadFile(with onUploadProgress)"
      contains: "import instance from '@/utils/request.ts'"
    - path: "price_recorder_vue/src/stores/mapStore.ts"
      provides: "Pinia setup-syntax store: spots[], selectedSpot, loading, filteredSpots computed, + save/update/delete/list/uploadPhoto/reverseGeocode actions with try/catch/finally + toast (mirror fuelStore)"
      contains: "defineStore('map'"
    - path: "price_recorder_vue/src/view/Map.vue"
      provides: "Full-screen Gaode map + capture bottom-sheet (D-15, D-17) + spot detail bottom-sheet (D-10) + FAB filter overlay (D-18) + empty-state geolocation fallback (D-20) + navigation deep-link handler"
      contains: "AMapLoader.load"
    - path: "price_recorder_vue/src/__tests__/stores/mapStore.spec.ts"
      provides: "Vitest assertions: listSpots populates spots; saveSpot calls api then re-lists; deleteSpot returns {success} AND local-mutates"
      contains: "vi.mock('@/api/map'"
    - path: "price_recorder_vue/src/__tests__/api/map.spec.ts"
      provides: "Vitest assertions: listSpots→GET /map/list/v1; saveSpot→POST /map/save/v1 serialized; getSpot→GET /map/get/<id> (PATH-TEMPLATE, not query); deleteSpot→POST /map/delete/v1 returns {success} not {flag}; reverseGeocode→POST /map/reverse-geocode/v1; uploadFile forwards onUploadProgress in 3rd-arg config"
      contains: "getMock, postMock"
    - path: "price_recorder_vue/src/__tests__/view/Map.spec.ts"
      provides: "Vue component spec: mocks window.AMapLoader/AMap/navigator.geolocation/URL.createObjectURL; asserts FAB tap → getCurrentPosition; marker click → selectedSpot; nav button → window.location.href = androidamap://; D-22 skip-list for live-map behaviors"
      contains: "vi.skipIf"
  key_links:
    - "AppLayout.vue navItems[5] {name:'map',path:'/map',label:'地图'} (D-14) ↔ router/index.ts children[/map] ↔ Map.vue → AMapLoader.load(import.meta.env.VITE_GAODE_JS_API_KEY) — link from nav tab to mounted map view"
    - "mapStore.spots ↔ Map.vue markers (WGS-84 raw → AMap.convertFrom → GCJ-02 marker position) — the contract-faithful conversion point; breakage causes 300-500m NE offset (Pitfall 1)"
    - "Map.vue capture flow → AMap.convertFrom(WGS-84→GCJ-02) BEFORE mapApi.reverseGeocode(GCJ-02 lng,lat) → backend regeo gets GCJ-02 not raw → accurate address — backend biz/map.go:85 has NO conversion (PINNED), so the frontend MUST pre-convert"

# Tests marked skipped (D-22) — real-device-only behaviors; re-routed to 13-VALIDATION.md Manual-Only section
skipped_tests:
  - id: SKIP-pin-clustering
    gate: "vi.skipIf(typeof (window as any).AMap?.MarkerClusterer !== 'function')"
    reason: "AMap.MarkerClusterer is a plugin loaded async via AMap.plugin(...); real-cluster aggregation of 50+ pins requires a live map instance (not available in jsdom)"
    manual_uat_ref: "13-VALIDATION.md ## Manual-Only — Pin clustering under 50+ spots"
  - id: SKIP-touch-pinch-zoom
    gate: "vi.skipIf(typeof (window as any).AMap?.Map !== 'function')"
    reason: "Touch-pinch / pan gestures need a real device with touch input and live tiles"
    manual_uat_ref: "13-VALIDATION.md ## Manual-Only — Mobile map interaction (pinch-zoom, pan, tap)"
  - id: SKIP-geolocation-denied-fallback
    gate: "vi.skipIf(typeof navigator?.geolocation?.getCurrentPosition !== 'function' || !('permissions' in navigator))"
    reason: "Real geolocation denial flow needs a device permission prompt + rejection; jsdom stubs always resolve"
    manual_uat_ref: "13-VALIDATION.md ## Manual-Only — Geolocation-denied fallback flow on a real phone"
  - id: SKIP-real-gps-accuracy
    gate: "vi.skipIf(typeof (window as any).AMap?.Map !== 'function')"
    reason: "Real 10-30m GPS accuracy in the field needs outdoor device GPS"
    manual_uat_ref: "13-VALIDATION.md ## Manual-Only — GPS capture in field accuracy"
  - id: SKIP-gaode-deep-link-opens-app
    gate: "vi.skipIf(typeof window !== 'undefined' && !/iPhone|iPad|iPod|Android/.test(navigator.userAgent))"
    reason: "Deep-link actually opens Gaode App requires app installed on test device"
    manual_uat_ref: "13-VALIDATION.md ## Manual-Only — Gaode deep-link navigation opens Gaode App"
  - id: SKIP-real-tile-render
    gate: "vi.skipIf(typeof (window as any).AMap?.Map !== 'function')"
    reason: "Real tile rendering + real AMap.Map DOM attachment needs network + valid JS API key + non-test DOM"
    manual_uat_ref: "13-VALIDATION.md ## Manual-Only — Real map tile rendering"
  - id: SKIP-https-geolocation-prod
    gate: "vi.skipIf(typeof window !== 'undefined' && window.location?.protocol !== 'https:' && location.hostname !== 'localhost')"
    reason: "Geolocation API requires HTTPS in production (Pitfall 3) — only network/origin-level testable"
    manual_uat_ref: "13-VALIDATION.md ## Manual-Only — Geolocation over HTTPS in production"

# Contract-fidelity findings gated from research artifacts (verified against live generated code)
contract_resolutions:
  - id: CR-1-getspot-route
    finding: "PATTERNS.md risk-flag #2 suggests mirroring fuel.ts getVehicleById query form `instance.get('/x/get/v1', {params:{id}})`. VERIFIED FALSE for the map domain: fuel.proto declares `get: /fuel/vehicle/get/v1` (no path-template) so query form works; map.proto declares `get: /map/get/{id}` (path-template) and blog/api/map/v1/map_http.pb.go:44 registers `r.GET(\"/map/get/{id}\", ...)`. Sending `/map/get/v1?id=5` would 404 (route `/map/get/{id}` requires a path segment, not `/v1`)."
    resolution: "map.ts getSpot(id) MUST use `instance.get('/map/get/' + encodeURIComponent(id))` — path-template form, NOT the query form. This is a verified contract override of PATTERNS.md risk-flag #2."
  - id: CR-2-reverse-geocode-preconvert
    finding: "blog/internal/biz/map.go:85 builds Gaode regeo `location=%f,%f` directly from the lng,lat args with NO WGS-84→GCJ-02 conversion (backend is PINNED, cannot modify). Gaode regeo expects GCJ-02; passing raw WGS-84 returns an address ~300-500m offset (Pitfall 1)."
    resolution: "Frontend MUST convert captured WGS-84 → GCJ-02 via AMap.convertFrom BEFORE calling mapApi.reverseGeocode for the display address. SpotEntity persisted via saveSpot stores RAW WGS-84 (RESEARCH Pattern 5). Display markers ALSO convert WGS-84→GCJ-02 client-side. Single unified conversion point in Map.vue capture flow."
  - id: CR-3-frontmatter-shape
    finding: "DeleteSpotReply is `{ success: bool }` (NOT `{ flag }` like fuel.ts:114). CreateSpotReply/UpdateSpotReply are `{ spot: SpotEntity }` (NOT SaveFuelReply `{ id, message }` like fuel.ts:53-56)."
    resolution: "map.ts deleteSpot returns `(... as {success:boolean}).success`; mapStore and Map.vue treat saveSpot/updateSpot reply as `{ spot: SpotEntity }` and use the returned spot entity directly."
  - id: CR-4-vitest-env-correction
    finding: "CONTEXT D-21 premise states 'Reuse happy-dom (already configured in vitest.config.ts)'. VERIFIED FALSE: vitest.config.ts:13 sets `environment: 'jsdom'`. happy-dom is NOT configured."
    resolution: "Honor D-21's INTENT ('don't introduce a second environment') by KEPT using the existing jsdom. Do NOT switch to happy-dom — switching would itself violate 'don't introduce a second env'. Context前提错误, 意图保留."

goal_closes_verification_gaps: [1, 2, 3, 4]   # The 4 FAILED truths in 13-VERIFICATION.md (SC-1..SC-4)
---

<objective>
Plan 13-03 closes the 4 FAILED user-facing truths in `13-VERIFICATION.md` by building the deferred frontend half of Phase 13: a Vue 3 Gaode map view with capture (SC-1), spot CRUD UI + bottom sheets (SC-2), navigation deep-links (SC-3), and Vitest coverage + mobile UAT reroute (SC-4).

Per D-01..D-22 in `13-CONTEXT.md`, this plan adds a 6th nav tab "地图", a full-screen Gaode map with pins for all saved spots, a bottom-sheet capture form (name-required per D-09/D-17, expandable notes/tags/photos), a multi-photo picker with parallel per-file progress (D-16), a FAB-toggled filter+search overlay (D-18), an empty-state geolocation fallback (D-20), and three Vitest specs that mock the AMap/globals/Geolocation stack (D-21/D-22).

Per `13-CONTEXT.md <specifics>` (line 149), this plan ALSO closes the broken requirement traceability hop flagged by VERIFICATION: register MAP-01/02/03 in `.planning/REQUIREMENTS.md` Active section.

Backend (`blog/`) is SHIPPED and PINNED via 13-01/13-02 — Plan 13-03 MUST NOT modify anything under `blog/`. All endpoints are live and JWT-protected.

Purpose: Make the backend map contract reachable by a user; ship the deferred frontend; close the 4 verification gaps.
Output: `price_recorder_vue/src/{api/map.ts, stores/mapStore.ts, view/Map.vue}` + 3 Vitest specs + nav/route wiring + `.env.example` + REQUIREMENTS/VALIDATION doc re-registration.
</objective>

<execution_context>
@/Users/hukss/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/hukss/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/13-fishing-spot-map/13-CONTEXT.md
@.planning/phases/13-fishing-spot-map/13-RESEARCH.md
@.planning/phases/13-fishing-spot-map/13-PATTERNS.md
@.planning/phases/13-fishing-spot-map/13-01-SUMMARY.md
@.planning/phases/13-fishing-spot-map/13-02-SUMMARY.md
@.planning/phases/13-fishing-spot-map/13-VERIFICATION.md
@.planning/phases/13-fishing-spot-map/13-VALIDATION.md

# PINNED backend contract — the generated HTTP routes & reply shapes the frontend MUST target
@blog/api/map/v1/map.proto
@blog/api/map/v1/map_http.pb.go
@blog/internal/biz/map.go
@blog/api/file/v1/file.proto

# Frontend analogs — mirror these for new files (per PATTERNS.md)
@price_recorder_vue/src/api/fuel.ts
@price_recorder_vue/src/api/appVersion.ts
@price_recorder_vue/src/stores/fuelStore.ts
@price_recorder_vue/src/view/DebtDetail.vue
@price_recorder_vue/src/utils/request.ts
@price_recorder_vue/src/components/AppLayout.vue
@price_recorder_vue/src/router/index.ts
@price_recorder_vue/vitest.config.ts
@price_recorder_vue/index.html
@price_recorder_vue/src/__tests__/stores/fuelStore.spec.ts
@price_recorder_vue/src/__tests__/api/fuel.spec.ts
@price_recorder_vue/src/__tests__/view/DebtDetail.spec.ts

# Skill: test-task authors follow .claude/skills/gen-test/SKILL.md conventions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register MAP-01/02/03 in REQUIREMENTS + extend 13-VALIDATION manual UAT (gap-closure)</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/phases/13-fishing-spot-map/13-VALIDATION.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md (entry shapes API-01..DOC-04 + Traceability table),
    .planning/phases/13-fishing-spot-map/13-VALIDATION.md (frontmatter + Manual-Only section lines 59-67 + per-task map line 41)
  </read_first>
  <action>
Per CONTEXT.md `<specifics>` line 149 and frontmatter `gap_closure: true`: VERIFICATION flagged a broken traceability hop — MAP-01/02/03 appear in ROADMAP.md Phase 13 but were never registered in REQUIREMENTS.md. Close it now (per D-22 route manual UAT items here too).

(1) In `.planning/REQUIREMENTS.md`: insert a new top-level `## Fishing Spot Map` section (between `## Contributor Documentation` and `---` / `## Traceability`) with three unchecked list rows mirroring the API-* / TEST-* / DOC-* shape:
- `[ ] **MAP-01**: User can capture and save current GPS coordinates with a name/note (Gaode JS API map + Browser Geolocation + backend /map/save/v1).`
- `[ ] **MAP-02**: User can view, edit, and delete saved spots (full-screen map with markers + bottom-sheet + ListSpots/UpdateSpot/DeleteSpot API).`
- `[ ] **MAP-03**: User can view spots on a map and navigate back to them (Gaode JS API markers + androidamap://navi / iosamap://navi deep-link).`
Then in the `## Traceability` table, append three rows: `MAP-01 | 13`, `MAP-02 | 13`, `MAP-03 | 13`. Use `Edit` (scoped replacement targeted at the `## Contributor Documentation` closing block and the Traceability table tail) — do NOT `Write` the entire file.

(2) In `.planning/phases/13-fishing-spot-map/13-VALIDATION.md`: extend the `## Manual-Only Verifications` table (currently lines 63-66 with 4 manual rows) to add the 7 D-22 skip-list items re-routed from the Plan frontmatter `skipped_tests` block. Each row: Behavior | Requirement | Why Manual | Test Instructions. Add rows for: Pin clustering under 50+ spots, Mobile map interaction (pinch-zoom, pan, tap) [merge with existing Mobile map interaction row if duplicate], Geolocation-denied fallback flow on a real phone, GPS capture in field accuracy (10-30m), Gaode deep-link navigation opens Gaode App [merge with the existing Gaode deep-link navigation row], Real map tile rendering, Geolocation over HTTPS in production. Also update the `## Per-Task Verification Map` table by replacing the placeholder row with the row IDs to be produced by Tasks 2-6 (MAP-01..03 + threat refs T-13-FE-*). Set `status: draft` unchanged; leave the `nyquist_compliant: false` and Wave 0 checklist as-is — the executor for the coding tasks will flip those after their tasks pass. Use `Edit` (scoped), not `Write`.

This task is a pure docs edit and is INTENTIONALLY Task 1 so the traceability hop is closed before any code is written (auditor sees the registration before the deliverables). No commits land on `blog/`. Atomic commit message: `docs(13-03): register MAP-01..03 in REQUIREMENTS + extend manual UAT (gap-closure part 1)`.
  </action>
  <verify>
    <automated>grep -q 'MAP-01' .planning/REQUIREMENTS.md && grep -q 'MAP-02' .planning/REQUIREMENTS.md && grep -q 'MAP-03' .planning/REQUIREMENTS.md && grep -qE 'MAP-0[123] \| 13' .planning/REQUIREMENTS.md && grep -q 'Pin clustering under 50\|Geolocation-denied fallback\|deep-link navigation opens Gaode App' .planning/phases/13-fishing-spot-map/13-VALIDATION.md && echo OK</automated>
  </verify>
  <done>
- `## Fishing Spot Map` section exists in REQUIREMENTS.md with MAP-01/02/03 rows.
- Traceability table has 3 new MAP-13 rows (no other rows changed).
- 13-VALIDATION.md Manual-Only table extended with all 7 D-22 skip-list behaviors re-routed.
- No `blog/` files touched.
  </done>
</task>

<task type="auto" autonomous="false">
  <name>Task 2: Create map.ts API client + .env.example + index.html security-config (AMap env bootstrap)</name>
  <files>
    price_recorder_vue/src/api/map.ts,
    price_recorder_vue/.env.example,
    price_recorder_vue/index.html
  </files>
  <read_first>
    price_recorder_vue/src/api/fuel.ts (decimalToString + serializeX + per-RPC function shape + delete returns {flag}; lines 1, 53-56, 69-72, 91-116, 141-144),
    price_recorder_vue/src/api/appVersion.ts (uploadFile pattern + delete returns {success}; lines 47-55),
    price_recorder_vue/src/utils/request.ts (instance interceptor unwraps body.data, forwards 3rd-arg config to axios; full file),
    blog/api/map/v1/map.proto (SpotEntity + CreateSpotRequest/Reply shapes),
    blog/api/map/v1/map_http.pb.go (lines 38-46: r.GET "/map/get/{id}" PATH-template, NOT query — gate CR-1)
  </read_first>
  <action>
Mirror `fuel.ts`'s per-RPC exported function pattern and `appVersion.ts:51-55` upload pattern, with these CONTRACT-FAITHFUL divergences locked from the generated backend (CR-1, CR-3):

Export interfaces: `SpotEntity` (id, name, latitude, longitude, notes, tags, photos: string[], address, createdAt, updatedAt — mirror field names from map.proto SpotEntity proto3 camelCase in JSON). `CreateSpotRequest = Omit<SpotEntity, 'id' | 'createdAt' | 'updatedAt'>` (mirror fuel.ts:58-67 Omit shape). `UpdateSpotRequest = Omit<SpotEntity, 'createdAt' | 'updatedAt'>` (keeps id).

Imports: `import instance from '@/utils/request.ts'` (default import, NOT named — same as fuel.ts:1).

Add `function decimalToString(value: string | number | undefined): string { if (value === undefined || value === null) return ''; return String(value) }` mirroring fuel.ts:69-72 verbatim — SpotEntity lat/lng are proto doubles arriving as numbers, but serialize via decimalToString to match backend's string↔float64 strconv round-trip documented in 13-02 SUMMARY and to mirror fuel.ts.

Add `function serializeSpot(data: CreateSpotRequest | UpdateSpotRequest)` returning `{ ...data, latitude: decimalToString(data.latitude), longitude: decimalToString(data.longitude) }` — ONLY lat/lng as strings; tags stays string; photos stays string[].

Export functions (every one `Promise<...>` typed to what the response interceptor returns — request.ts:9-11 unwraps `body.data`, so these return the inner proto reply object, NOT wrapped):
- `listSpots(): Promise<{ spots: SpotEntity[] }>` → `instance.get('/map/list/v1')` — ListSpotsRequest is empty, no params.
- `getSpot(id: string): Promise<{ spot: SpotEntity }>` → `instance.get('/map/get/' + encodeURIComponent(id))` — **CR-1: PATH-TEMPLATE, NOT the query form.** map_http.pb.go:44 registers `/map/get/{id}`; do NOT use `{params:{id}}` (would 404).
- `saveSpot(data: CreateSpotRequest): Promise<{ spot: SpotEntity }>` → `instance.post('/map/save/v1', serializeSpot(data))` — **CR-3: reply is `{spot}`, NOT SaveFuelReply `{id, message}`.**
- `updateSpot(data: UpdateSpotRequest): Promise<{ spot: SpotEntity }>` → `instance.post('/map/update/v1', serializeSpot(data))`.
- `deleteSpot(id: string): Promise<boolean>` → `const reply = (await instance.post('/map/delete/v1', { id })) as { success: boolean }; return reply.success` — **CR-3: `{success}`, NOT fuel's `{flag}` (PATTERNS risk #1).** Mirror appVersion.ts:47-49 shape conceptually.
- `reverseGeocode(input: { latitude: number; longitude: number }): Promise<{ address: string }>` → `instance.post('/map/reverse-geocode/v1', { latitude: input.latitude, longitude: input.longitude })` — JSON key order is irrelevant; include BOTH keys (per PATTERNS.md risk-flag #3 — wire order is a code-style convention, JSON is unordered). Frontend caller (Map.vue) passes ALREADY-CONVERTED GCJ-02 coords per CR-2.
- `uploadSpotPhoto(file: File, onUploadProgress?: (e: any) => void): Promise<{ id: string; url: string }>` → mirror appVersion.ts:51-55 but pass `instance.post('/file/upload/raw/v1', formData, onUploadProgress ? { onUploadProgress } : undefined)` as the third arg. request.ts:5 axios `instance.post(url, data, config?)` forwards `config` verbatim — interceptor pipeline doesn't strip it (verified by reading request.ts:6-28). No modifications to request.ts.

DO NOT modify `src/utils/request.ts` — it forwards the third axios arg verbatim (verified). PATTERNS.md risk-flag #7 says verify with a map.spec.ts assertion in Task 6. If upload-with-progress breaks at runtime, the fix is a one-line pass-through in request.ts; do NOT preempt that fix.

Create `.env.example` (NEW file — no `.env*` exists in price_recorder_vue/, verified by glob):
```
# Gaode (AMap) Web端 JS API — distinct from the backend GAODE_WEB_API_KEY (Web服务, server-side ONLY).
# Get these from https://console.amap.com/dev/key/app → 创建「Web端(JS API)」应用.
# The JS API key is frontend-exposed (domain-restricted); the security code is a browser-side verification key.
VITE_GAODE_JS_API_KEY=your_web_js_api_key
VITE_GAODE_JS_SECURITY_CODE=your_js_security_code
# NOTE: VITE_ prefix is REQUIRED — Vite only exposes env vars prefixed with VITE_ to import.meta.env.
# Add localhost + production domain to the JS API key's domain whitelist in the Gaode console (Pitfall 4) — else blank tiles.
# Geolocation API requires HTTPS in production (Pitfall 3); localhost is whitelisted.
```

Edit `price_recorder_vue/index.html`: in `<head>` after the stylesheet link and BEFORE `<script type="module" src="/src/main.ts">`, add a small inline `<script>` that sets `window._AMapSecurityConfig = { securityJsCode: import.meta.env.VITE_GAODE_JS_SECURITY_CODE }` — BUT inline HTML cannot read import.meta.env at runtime (index.html is not module-processed). Use the DOCUMENTED PATTERN (RESEARCH Pattern 3 lines 329-333): inject `<script>window._AMapSecurityConfig = { securityJsCode: '「YOUR_SECURITY_CODE」' };</script>` as a literal placeholder in index.html is wrong (secrets in repo). Instead, set `window._AMapSecurityConfig` at the TOP of Map.vue `<script setup>` module scope (NOT inside onMounted — must run before AMapLoader.load per PATTERNS risk-flag #5). So: index.html ADD ONLY the CDN loader script tag `<script src="https://webapi.amap.com/loader.js"></script>` in `<head>` (before main.ts). Document in index.html a comment: `<!-- AMapLoader exposed as window.AMapLoader — security code set in Map.vue module scope per RESEARCH Pattern 3 -->`. Do NOT put the key in index.html.
  </action>
  <verify>
    <automated>cd price_recorder_vue && pnpm vue-tsc --noEmit 2>&1 | tail -5; grep -q "instance.get('/map/get/'" src/api/map.ts && grep -q 'success: boolean' src/api/map.ts && grep -q 'onUploadProgress' src/api/map.ts && grep -q 'VITE_GAODE_JS_API_KEY' .env.example && grep -q 'webapi.amap.com/loader.js' index.html && ! grep -qE 'flag:\s*boolean' src/api/map.ts && echo OK</automated>
  </verify>
  <done>
- `src/api/map.ts` exists, exports all 7 functions, types match proto reply shapes.
- `getSpot` uses path-template `/map/get/${encodeURIComponent(id)}` (CR-1 — not query form).
- `deleteSpot` casts reply to `{success:boolean}` (CR-3 — not `{flag}`).
- `saveSpot`/`updateSpot` typed as `Promise<{spot: SpotEntity}>` (CR-3 — not SaveFuelReply).
- `uploadSpotPhoto` forwards `onUploadProgress` in 3rd-arg axios config (untested in repo — Task 6 verifies).
- `.env.example` has both VITE_ vars with Gaode console URL comment.
- `index.html` has `webapi.amap.com/loader.js` script tag; NO secrets in index.html.
- `request.ts` UNCHANGED.
- `pnpm tsc --noEmit` exits 0 (or only pre-existing unrelated errors).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire /map route + add 地图 nav tab to AppLayout (D-14)</name>
  <files>
    price_recorder_vue/src/router/index.ts,
    price_recorder_vue/src/components/AppLayout.vue
  </files>
  <read_first>
    price_recorder_vue/src/router/index.ts (children array under path '/', line 26-69; auth guard lines 74-90),
    price_recorder_vue/src/components/AppLayout.vue (navItems array lines 12-18, currentRouteName computed line 20, v-for lines 57 & 125)
  </read_first>
  <action>
Per D-14: append `{ name: 'map', path: '/map', label: '地图' }` as the 6th entry of `navItems` in `AppLayout.vue` (after the appVersion entry at line 18). Use `Edit` scoped to the navItems block (lines 12-18). The `currentRouteName` computed (line 20) and the v-for loops at lines 57 and 125 already generically render ANY navItems entry — zero other edits. Do NOT collapse other tabs; do NOT replace 版本; mobile hamburger menu auto-includes the 6th entry.

Per D-14 (router): in `price_recorder_vue/src/router/index.ts`, add a sibling child under the `/` parent's `children` array (lines 26-69), mirroring the existing entries (BlogList, FuelList, etc.):
`{ name: 'map', path: 'map', component: () => import('@/view/Map.vue'), meta: { requiresAuth: true } },`
Place it after the appVersion route entry (lines 63-68) — append before the closing `]` of the children array. `Edit` scoped. Auth guard at lines 74-90 already protects all children of `/` (parent has `requiresAuth: true` at line 24) — no guard changes needed.

Atomic commit: `feat(13-03): wire /map route and 地图 nav tab (D-14)`.
  </action>
  <verify>
    <automated>cd price_recorder_vue && grep -qE "name: 'map'" src/router/index.ts && grep -qE "name: 'map'.*path: '/map'.*label: '地图'" src/components/AppLayout.vue && grep -qE "import.*Map\.vue" src/router/index.ts && echo OK</automated>
  </verify>
  <done>
- AppLayout.vue navItems has 6 entries; the 6th is the map/地图 entry (D-14).
- router/index.ts children[] has a new `/map → Map.vue` route with requiresAuth:true.
- No other routes or guards changed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create mapStore.ts Pinia store (mirror fuelStore)</name>
  <files>
    price_recorder_vue/src/stores/mapStore.ts
  </files>
  <read_first>
    price_recorder_vue/src/stores/fuelStore.ts (210 lines — setup-syntax skeleton lines 7-21, async action+toast mutation 60-72, delete local-mutation 91-104, return block 181-209),
    price_recorder_vue/src/api/map.ts (Task 2 output — exported function signatures & reply shapes)
  </read_first>
  <action>
Mirror `fuelStore.ts`'s setup-syntax Pinia store pattern exactly (per PATTERNS.md). `defineStore('map', () => { ... })`.

State refs (mirror fuelStore.ts:7-21):
- `spots: ref<SpotEntity[]>([])`
- `selectedSpot: ref<SpotEntity | null>(null)` — for spot detail bottom-sheet (D-10)
- `loading: ref(false)`
- `error: ref<string | null>(null)`
- `uploadingPhotos: ref<Array<{ url: string; progress: number; name: string }>>([])` — D-16 in-progress uploads
- `selectedTags: ref<string[]>([])` and `searchQuery: ref<string>('')` — D-12/D-19 filter state

Computed (return block):
- `spotCount = computed(() => spots.value.length)`
- `filteredSpots = computed(() => spots filtered by selectedTags (substring match in spot.tags split by comma) AND searchQuery (case-insensitive substring in spot.name))` — D-12/D-19 client-side filter
- `allTags = computed(() => [[...new Set(spots.flatMap(s => (s.tags || '').split(',').map(t => t.trim()).filter(Boolean)))]])` — chip row source

Actions (every one wrapping try/catch/finally with `loading.value=true → try { await mapApi.X; toast.success('...') } catch { toast.error; throw } finally { loading.value=false }` per fuelStore.ts:60-72):
- `fetchSpots()` → `spots.value = (await mapApi.listSpots()).spots` (re-fetch after save per createVehicle pattern at fuelStore.ts:60-72).
- `saveSpot(data: CreateSpotRequest)` → `await mapApi.saveSpot(data)`; toast '位置已保存'; `await fetchSpots()`.
- `updateSpot(data: UpdateSpotRequest)` → `await mapApi.updateSpot(data)`; toast '位置已更新'; `await fetchSpots()`.
- `deleteSpot(id: string)` → `const ok = await mapApi.deleteSpot(id)`; toast '位置已删除'; local-mutate `spots.value = spots.value.filter(s => s.id !== id)` (mirror deleteVehicle at fuelStore.ts:91-104 — local mutation preferred over re-fetch for snappy UI). NOTE delete returns boolean via mapApi (CR-3).
- `getSpot(id: string)` → `(await mapApi.getSpot(id)).spot` (read-only — no toast needed).
- `reverseGeocode(input: { latitude: number; longitude: number })` → `(await mapApi.reverseGeocode(input)).address` (read-only display call; toast only on error). NOTE per CR-2 the caller (Map.vue) MUST pass already-converted GCJ-02 coords; the store is coordinate-agnostic.
- `uploadSpotPhoto(file: File)` → push `{ name: file.name, progress: 0, url: '' }` to `uploadingPhotos`; `await mapApi.uploadSpotPhoto(file, (e) => { entry.progress = Math.round((e.loaded/e.total)*100) })`; on success replace entry.url with returned url; on failure remove entry + toast error. Mirror D-16: parallel uploads triggered on file selection, not on spot-submit. The successful URLs are concatenated into `CreateSpotRequest.photos` by Map.vue at submit time.
- `setSelectedSpot(spot | null)` — opens/closes bottom-sheet (D-10).
- `setFilter({tags?, query?})` — updates selectedTags / searchQuery (D-19).

Return all state refs + computed + actions. Toast via `useToast()` from vue-toastification (mirror fuelStore.ts:130).

Import `import * as mapApi from '@/api/map'` and `import type { SpotEntity, CreateSpotRequest, UpdateSpotRequest } from '@/api/map'` (mirror fuelStore.ts:131-132).

Atomic commit: `feat(13-03): add mapStore Pinia store (mirrors fuelStore with deleteUrl-list mutate + filter+upload state)`.
  </action>
  <verify>
    <automated>cd price_recorder_vue && pnpm vue-tsc --noEmit 2>&1 | grep -c "stores/mapStore" | grep -q '^0$' && grep -q "defineStore('map'" src/stores/mapStore.ts && grep -q 'uploadingPhotos' src/stores/mapStore.ts && grep -q 'filteredSpots' src/stores/mapStore.ts && echo OK</automated>
  </verify>
  <done>
- `defineStore('map', ...)` setup-syntax store exists with all listed state/computed/actions.
- deleteSpot local-mutates `spots` (filter) — NOT re-fetch (mirror fuelStore.deleteVehicle).
- saveSpot/updateSpot toast success then re-fetch list.
- uploadingPhotos supports per-file progress (D-16).
- filteredSpots computed handles tag-chip + search (D-12/D-19).
- pnpm tsc --noEmit exit 0 for mapStore.ts.
  </done>
</task>

<task type="auto" autonomous="false">
  <name>Task 5: Create Map.vue (full-screen Gaode map + capture bottom-sheet + filter FAB + empty-state + nav deep-link)</name>
  <files>
    price_recorder_vue/src/view/Map.vue
  </files>
  <read_first>
    price_recorder_vue/src/view/DebtDetail.vue (modal+transition pattern lines 280-326; form+submit+isSubmitting lines 17-19,56-72; file-input change handler lines 101-131),
    .planning/phases/13-fishing-spot-map/13-RESEARCH.md (Pattern 3 AMap loader lines 321-353, Pattern 5 convertFrom lines 368-389, Pattern 6 deep-link lines 393-405, Code Examples lines 503-560 & 619-640),
    .planning/phases/13-fishing-spot-map/13-PATTERNS.md (Analog B Map.vue AMap skeleton lines 239-304; debt modal adaptation lines 186-218),
    price_recorder_vue/src/stores/mapStore.ts (Task 4 output — exported state/actions),
    price_recorder_vue/src/api/map.ts (Task 2 output — CreateSpotRequest/SpotEntity shapes)
  </read_first>
  <action>
Build a Vue 3 `<script setup lang="ts">` SFC. Template structure (Tailwind):

**Module scope (TOP of `<script setup>`, BEFORE any onMounted — per PATTERNS risk-flag #5, security config must be set before AMapLoader.load):**
Set `window._AMapSecurityConfig = { securityJsCode: import.meta.env.VITE_GAODE_JS_SECURITY_CODE }` ONLY if the env var is non-empty (guards the smoke-test path when the key is missing during unit tests).

**Refs:** `mapInstance = ref<any>(null)`, `markers = ref<Map<string, any>>(new Map())`, `AMapNS: any = null` (held module-scope after loader resolves), `captureOpen = ref(false)`, `editingSpot = ref<SpotEntity|null>(null)`, `isSubmitting = ref(false)`, `showExpandedForm = ref(false)` (D-17 chevron), `filterOpen = ref(false)` (D-18), `capturedAddress = ref('')`, `capturedWgs84 = ref<{lng:number,lat:number}|null>(null)`, `capturedGcj02 = ref<{lng:number,lat:number}|null>(null)`, `showEmptyTip = ref(true)` (D-20).

**Form refs:** `name = ref('')`, `notes = ref('')`, `tags = ref('')`, `photoPreviews = ref<{url:string,name:string,progress:number}[]>([])`.

**onMounted** (mirror RESEARCH Pattern 3 lines 336-353):
1. Acquire AMap: `AMapNS = await (window as any).AMapLoader.load({ key: import.meta.env.VITE_GAODE_JS_API_KEY, version: '2.0', plugins: ['AMap.Geolocation', 'AMap.MarkerClusterer'] })`. The `plugins: [...]` form asks AMapLoader to pre-load the Geolocation plugin (D-20 uses it) and MarkerClusterer (kept available per D-22 risk-flag #6 — do NOT instantiate the clusterer in 13-03; defer to next phase per the planning_context note. Just make the plugin available; the verifier gate SKIP-pin-clustering covers it).
2. Initialize map instance with the empty-state fallback (D-20): if `mapStore.spots.length === 0`, attempt `AMap.Geolocation` plugin `getCurrentPosition` (high-accuracy, 10s timeout); on success center on the returned GCJ-converted coords; on failure center on the last-known spot's GCJ-02 position if any, else center on `[113.264385, 23.129112]` Guangzhou default (GCJ-02 directly — no conversion for this static fallback per D-20).
3. `await mapStore.fetchSpots()`; for each spot call `addSpotMarker(spot)` (below).
4. `showEmptyTip.value = mapStore.spots.length === 0` — keep the soft tooltip "点右上按钮保存当前钓点" visible until first save or first FAB open (D-20).

**addSpotMarker(spot)** (RESEARCH Code Examples 537-560 VERBATIM pattern, adapted): call `await convertWgs84ToGcj02(spot.longitude, spot.latitude)` (CR-2 — markers are GCJ-02) → `const marker = new AMapNS.Marker({ position: [gcj.lng, gcj.lat], title: spot.name, icon: new AMapNS.Icon({...default marker, no custom asset needed in v1... }) })` → `marker.on('click', () => { mapStore.setSelectedSpot(spot) })` → `mapInstance.value.add(marker)` → `markers.value.set(spot.id, marker)`.

**convertWgs84ToGcj02(lng,lat)** helper — RESEARCH Pattern 5 lines 376-388 VERBATIM (returns Promise<[lng,lat]>; rejects on `status !== 'complete' || result.info !== 'ok'`).

**Capture FAB handler (D-07, D-15)** — bottom-edge "+" button, NOT to be merged with filter FAB (per CONTEXT specifics line 148):
1. `navigator.geolocation.getCurrentPosition(async (pos) => { capturedWgs84.value = { lng: pos.coords.longitude, lat: pos.coords.latitude }; capturedGcj02.value = await convertWgs84ToGcj02(...); try { const reply = await mapStore.reverseGeocode(capturedGcj02.value); capturedAddress.value = reply.address } catch { capturedAddress.value = '' }; openCaptureSheet() }, (err) => { toast.error('无法获取位置'); /* geolocation-denied fallback per D-20: center on last-known or Guangzhou default — manual UAT per D-22 */ }, { enableHighAccuracy: true, timeout: 10000 })` — **CR-2: reverseGeocode is sent GCJ-02 coords (already converted), backend biz/map.go:85 does NO conversion.**
2. `openCaptureSheet()` → reset form refs → `captureOpen.value = true; editingSpot.value = null; showExpandedForm.value = false`.
3. `closeCaptureSheet()` → `captureOpen.value = false` (reset on close).

**Photo picker (D-16)** — `<input type="file" multiple accept="image/*" @change="handlePhotoChange">`:
Loop `input.files` in parallel via `Promise.allSettled`; for each call `mapStore.uploadSpotPhoto(file)` (the store wires the per-file progress callback); push responses into `photoPreviews`; show thumbnail + delete-x per item. In `finally` reset `input.value = ''` (so re-selecting the same file re-fires change — mirror DebtDetail.vue:130). Do NOT defer upload to spot submit (D-16 explicit).

**handleSubmitCaptureForm (D-09/D-17)**:
- required-field gate: `if (!name.value.trim()) return` (only name required — D-09).
- build `CreateSpotRequest` from `capturedWgs84.value` PER CR-2: **send RAW WGS-84 lat/lng** to saveSpot (backend stores raw); NOT the GCJ-02 — GCJ-02 is display-only. Pass `photos: photoPreviews.map(p => p.url)`, `tags: tags.value` (comma string), `notes: notes.value`, `address: capturedAddress.value`.
- `isSubmitting.value = true; try { await mapStore.saveSpot(payload); captureOpen.value = false; await mapStore.fetchSpots(); await addSpotMarker(mapStore.spots[mapStore.spots.length - 1]) /* or rely on fetchSpots re-rendering all markers */; showEmptyTip.value = false } finally { isSubmitting.value = false }`.

**Edit flow (D-11):** tapping Edit on a spot-bottom-sheet opens the capture bottom-sheet pre-filled — reuse `openCaptureSheet()` but populate name/notes/tags/photoPreviews and set `editingSpot.value = selectedSpot` so handleSubmitCaptureForm calls `mapStore.updateSpot({...payload, id: editingSpot.value.id})` instead of saveSpot.

**Delete flow (D-11):** confirm dialog (use `window.confirm('确定删除此钓点？')` — simple and consistent with the project's existing no-library approach per DebtDetail analog); on confirm call `mapStore.deleteSpot(selectedSpot.id)`; on success remove the marker (`mapInstance.value.remove(markers.value.get(id))`) and close sheet.

**Navigate button (D-03, D-10) — RESEARCH Code 623-640 VERBATIM:**
`function navigateToSpot(spot: SpotEntity) { /* convert raw WGS-84 to GCJ-02 here for the deep-link too — dev=0 expects GCJ-02 */ convertWgs84ToGcj02(spot.longitude, spot.latitude).then(([lng,lat]) => { const isIOS = /iPhone|iPad|iPod/.test(navigator.userAgent); const scheme = isIOS ? 'iosamap://' : 'androidamap://'; const uri = `${scheme}navi?sourceApplication=blog&poiname=${encodeURIComponent(spot.name)}&lat=${lat}&lon=${lng}&dev=0&style=2`; window.location.href = uri; setTimeout(() => { if (document.hasFocus()) window.open(`https://uri.amap.com/navigation?to=${lng},${lat},${encodeURIComponent(spot.name)}`, '_blank') }, 2000) }) }` — `dev=0` because frontend already converted (RESEARCH Pattern 6 deep-link section).

**Filter FAB (D-18):** top-right FAB → toggles `filterOpen.value`. The overlay slides DOWN (custom Tailwind transition: translate-y-[-100%]→translate-y-0). Overlay contains `<input v-model="searchQuery">` and a chip row from `mapStore.allTags` (toggle membership in `mapStore.selectedTags`). Per D-19: selecting a chip or submitting search HIDES non-matching markers (`markers.value.forEach((m, id) => { const spot = mapStore.spots.find(s => s.id === id); m.hide(!mapStore.filteredSpots.includes(spot)) })`) WITHOUT auto-pan or refit. A separate "fit view" toolbar button calls `mapInstance.value.setFitView()` — NOT auto-triggered (D-19). Closing the overlay returns to full-screen (D-18).

**Empty-state tooltip (D-20):** a soft bubble "点右上按钮保存当前钓点" positioned with `position: absolute; top: 70px; right: 16px` (below where the FABs live). Visibility gated by `showEmptyTip && mapStore.spots.length === 0`. Hide on first saveSuccess or first FAB open.

**onUnmounted cleanup** (RESEARCH Anti-Pattern 3): `mapInstance.value?.destroy()`.

Sinature: reuse `DebtDetail.vue` nested-`<Transition>` pattern (PATTERNS Analog A lines 186-196) but change inner panel: `items-center justify-center`→`items-end justify-center`, inner transition `translate-y-full`→`translate-y-0` enter-from (slides up from bottom). Overlay `@click.self="closeCaptureSheet"`.

Tailwind mobile-first: full-screen map container `h-[calc(100vh-56px)]` (minus the AppLayout nav height — match the AppLayout `<main>` calc), `w-full`, `relative`. Big touch targets (min 44px). Per AGENTS.md Vue convention: 2-space indent, PascalCase file.

This task is `autonomous="false"` because live AMap map rendering requires `VITE_GAODE_JS_API_KEY` + `VITE_GAODE_JS_SECURITY_CODE` in `.env.local`; the executor can build and run unit tests with stubbed AMap, but map tile render must be verified manually (re-routed to 13-VALIDATION Manual-Only).

Atomic commit: `feat(13-03): add Map.vue — Gaode map + capture bottom-sheet + filter FAB + empty-state + nav deep-link`.
  </action>
  <verify>
    <automated>cd price_recorder_vue && pnpm vue-tsc --noEmit 2>&1 | grep -c "view/Map.vue" | grep -q '^0$' && grep -q 'AMapLoader.load' src/view/Map.vue && grep -q 'convertWgs84ToGcj02' src/view/Map.vue && grep -q 'androidamap://' src/view/Map.vue && grep -q 'navigator.geolocation.getCurrentPosition' src/view/Map.vue && grep -q '_AMapSecurityConfig' src/view/Map.vue && echo OK</automated>
  </verify>
  <done>
- Map.vue renders `<div id="map-container">` full-screen under AppLayout.
- AMapLoader.load with import.meta.env keys + plugins:['AMap.Geolocation','AMap.MarkerClusterer'].
- window._AMapSecurityConfig set at module scope before loader (PATTERNS risk-flag #5 — NOT inside onMounted).
- Capture FAB calls navigator.geolocation.getCurrentPosition → convertWgs84ToGcj02 → mapStore.reverseGeocode (GCJ-02 coords per CR-2) → opens bottom-sheet with name + address chip (D-17).
- handleSubmitCaptureForm: name-only required gate (D-09); saveSpot called with RAW WGS-84 lat/lng (CR-2 — stored raw, displayed GCJ-02).
- Marker creation path converts WGS-84→GCJ-02 (Pitfall 1 mitigation).
- Delete flow: confirm dialog → mapStore.deleteSpot (returns success bool per CR-3) → marker removed.
- Navigate button: window.location.href set to androidamap://navi...dev=0 with GCJ-02 coords, 2s web fallback to uri.amap.com.
- Filter FAB (top-right) toggles slide-down overlay; chip/search HIDES non-matching markers WITHOUT auto-pan; separate "fit view" button calls setFitView (D-19).
- Empty-state bubble showEmptyTip visible when spots.length===0; hides on first save/FAB open (D-20).
- onUnmounted destroys mapInstance (Anti-Pattern 3).
- pnpm tsc --noEmit exit 0 for Map.vue.
  </done>
</task>

<task type="auto">
  <name>Task 6: Vitest — mapStore.spec.ts, map.spec.ts, Map.spec.ts (D-21/D-22)</name>
  <files>
    price_recorder_vue/src/__tests__/stores/mapStore.spec.ts,
    price_recorder_vue/src/__tests__/api/map.spec.ts,
    price_recorder_vue/src/__tests__/view/Map.spec.ts
  </files>
  <read_first>
    price_recorder_vue/src/__tests__/stores/fuelStore.spec.ts (89 lines — vi.mock vue-toastification, vi.mock '@/api/fuel', beforeEach setActivePinia + clearAllMocks, mockResolvedValue asserts toHaveBeenCalledWith),
    price_recorder_vue/src/__tests__/api/fuel.spec.ts (68 lines — getMock/postMock scaffolding, vi.mock('@/utils/request.ts'), decimalToString serialization assertions),
    price_recorder_vue/src/__tests__/view/DebtDetail.spec.ts (84 lines — @vue/test-utils mount + flushPromises, vue-router/vue-toastification vi.mock spy, file-input files mutation + change trigger),
    price_recorder_vue/src/api/map.ts (Task 2),
    price_recorder_vue/src/stores/mapStore.ts (Task 4),
    price_recorder_vue/vitest.config.ts (environment: 'jsdom' — CR-4: do NOT switch to happy-dom),
    .claude/skills/gen-test/SKILL.md (project test convention: Vitest + @vue/test-utils, specs under src/__tests__/)
  </read_first>
  <action>
Per D-21 + gen-test skill: ship three Vitest specs. CR-4: keep `environment: 'jsdom'` (vitest.config.ts:13) — do NOT introduce happy-dom as a second env (D-21 intent).

**`src/__tests__/api/map.spec.ts`** (mirror fuel.spec.ts 1:1 scaffolding):
`getMock = vi.fn()`, `postMock = vi.fn()`, `vi.mock('@/utils/request.ts', () => ({ default: { get: getMock, post: postMock } }))`, `beforeEach { vi.clearAllMocks(); postMock.mockResolvedValue({}); getMock.mockResolvedValue({}) }`.
Tests:
- `listSpots()` calls `getMock` with `'/map/list/v1'` (no params arg).
- `saveSpot({name:'X', latitude:23.1, longitude:113.2, notes:'', tags:'', photos:[], address:''})` calls `postMock` with `'/map/save/v1'` AND serialized payload `{name:'X', latitude:'23.1', longitude:'113.2', notes:'', tags:'', photos:[], address:''}` (decimalToString converts lat/lng to strings — mirror fuel.spec.ts:408-414).
- `getSpot('5')` calls `getMock` with `'/map/get/5'` — **CR-1 assertion: PATH-TEMPLATE, not `/map/get/v1`+params.** This is the regression guard against the PATTERNS.md risk-flag #2 mistake.
- `deleteSpot('5')` calls `postMock` with `'/map/delete/v1'`, `{ id: '5' }` — AND assert the function returns `.success` (cast as `{success:boolean}`), NOT `.flag`. Provide a mockResolvedValue `{ success: true }` and `expect(await deleteSpot('5')).toBe(true)`.
- `reverseGeocode({latitude:23.1, longitude:113.2})` calls `postMock` with `'/map/reverse-geocode/v1'`, `{ latitude: 23.1, longitude: 113.2 }`.
- `uploadSpotPhoto(file, onProgress)` calls `postMock` with `'/file/upload/raw/v1'`, `formData` (instanceof FormData), AND a **third arg config containing `onUploadProgress`** — the regression guard for PATTERNS risk-flag #7 (request.ts forwarding is otherwise untested).

**`src/__tests__/stores/mapStore.spec.ts`** (mirror fuelStore.spec.ts 1:1):
`vi.mock('vue-toastification', () => ({ useToast: () => ({ success: vi.fn(), error: vi.fn() }) }))`, `vi.mock('@/api/map', () => ({ listSpots: vi.fn(), getSpot: vi.fn(), saveSpot: vi.fn(), updateSpot: vi.fn(), deleteSpot: vi.fn(), reverseGeocode: vi.fn(), uploadSpotPhoto: vi.fn() }))`, `beforeEach { setActivePinia(createPinia()); vi.clearAllMocks() }`.
Tests:
- `fetchSpots` populates `store.spots` from `listSpots()` reply `.spots` and asserts call signature.
- `saveSpot` calls api then re-fetches (assert `mapApi.saveSpot` called with payload, then `mapApi.listSpots` called — mirror createVehicle).
- `deleteSpot('5')` calls api; `mapApi.deleteSpot` mock resolves `{ success: true }` (CR-3); store local-mutates `spots` filter; `expect(store.spots.find(s => s.id === '5')).toBeUndefined()`.
- `uploadSpotPhoto` pushes a `{url,progress,name}` entry and on api resolves replaces `.url` — assert the entry's url transitions from `''` to the mock url.

**`src/__tests__/view/Map.spec.ts`** (the new pattern — DebtDetail.spec.ts gives the mount scaffolding, fuelStore.spec.ts gives the mock pattern, D-22 gives the skip-If strategy):
Top-of-file comment block enumerating the D-22 skip list with refs to 13-VALIDATION.md Manual-Only (mirror the Plan frontmatter `skipped_tests` entries).
`vi.mock('vue-router', () => ({ useRoute: () => ({ name: 'map' }), useRouter: () => ({ push: vi.fn() }) }))`, `vi.mock('vue-toastification', ...)` returning stub, `vi.mock('@/stores/mapStore', ...)` returning a stub store with `vi.fn()` actions and `ref()` state.
Mock the AMap globals: `beforeEach { window.AMapLoader = { load: vi.fn().mockResolvedValue({ Map: vi.fn(), Marker: vi.fn(), Icon: vi.fn(), Size: vi.fn(), convertFrom: vi.fn((coords, type, cb) => cb('complete', { info: 'ok', locations: [{lng:113.26,lat:23.13}] })), Geolocation: vi.fn(), plugin: vi.fn((name, cb) => cb && cb()) }) }; window.AMap = { Map: vi.fn().mockImplementation(() => ({ add: vi.fn(), remove: vi.fn(), destroy: vi.fn(), setFitView: vi.fn(), setCenter: vi.fn() })), Marker: vi.fn().mockImplementation(() => ({ on: vi.fn(), getPosition: vi.fn() })), convertFrom: vi.fn((coords, type, cb) => cb('complete', { info: 'ok', locations: [{lng:113.26, lat:23.13}] })) }; vi.stubGlobal('navigator', { geolocation: { getCurrentPosition: vi.fn((success) => success({ coords: { longitude: 113.265, latitude: 23.129, accuracy: 10 } })) } }); vi.stubGlobal('URL', { createObjectURL: vi.fn().mockReturnValue('blob:stub') }) }`.
Mount with `@vue/test-utils` `mount(Map, { global: { plugins: [pinia] } })` + `flushPromises()` after mount (let onMounted async settle — mirror DebtDetail.spec.ts:66-67).
Tests:
- FAB capture click → `navigator.geolocation.getCurrentPosition` called (assert via the stubbed mock).
- **CR-2 order guard (T-13-FE-05):** assert `vi.mocked(window.AMap.convertFrom)` was invoked BEFORE `mapStore.reverseGeocode` in the capture flow — use `expect(mapStore.reverseGeocode).toHaveBeenCalledAfter(vi.mocked(window.AMap.convertFrom))` (vitest 4.x supports this for spies on the same mock registry). This is the regression guard against the 300-500m NE offset bug if a future refactor skips or reorders the pre-conversion.
- After converting WGS-84→GCJ-02 and calling `reverseGeocode`, the address chip renders the mock address (mock mapStore.reverseGeocode to resolve `{address:'测试地址'}`).
- Marker click → `mapStore.setSelectedSpot(spot)` called (assert the store stub's setSelectedSpot mock was invoked).
- Navigate button click → `window.location.href` set to a string starting with `androidamap://navi` and containing `dev=0` — but jsdom may block location.href writes; assert by spying on `window.location.assign` OR by mocking window.location via a writable spy.
- `handlePhotoChange` with two File inputs → `mapStore.uploadSpotPhoto` called twice with file + onProgress callback (D-16 parallelism).
- `showEmptyTip` true on mount when `mapStore.spots.length === 0`; false after a successful save.
- D-22 skip-list tests: 7 `it.skipIf(...)` blocks matching the Plan frontmatter `skipped_tests` — each asserting its manual-UAT-redirected behavior; body throws or asserts live behavior. Comment block at top of file MUST list all 7 skip IDs and their `13-VALIDATION.md ## Manual-Only` rows.

Reuse existing jsdom env (vitest.config.ts:13). Do NOT switch to happy-dom (CR-4 — happy-dom isn't even installed). Do NOT add jsdom as a second env (it IS the env).
  </action>
  <verify>
    <automated>cd price_recorder_vue && pnpm test:unit --run -- src/__tests__/api/map.spec.ts src/__tests__/stores/mapStore.spec.ts src/__tests__/view/Map.spec.ts 2>&1 | tail -15; grep -q "vi.mock('@/utils/request.ts'" src/__tests__/api/map.spec.ts && grep -q "'/map/get/5'" src/__tests__/api/map.spec.ts && grep -q 'success: true' src/__tests__/stores/mapStore.spec.ts && grep -q 'vi.skipIf' src/__tests__/view/Map.spec.ts && grep -q 'androidamap://' src/__tests__/view/Map.spec.ts</automated>
  </verify>
  <done>
- 3 spec files exist; `pnpm test:unit --run` for these 3 passes.
- map.spec.ts asserts getSpot uses path-template `/map/get/5` (CR-1 regression guard).
- map.spec.ts asserts deleteSpot reply cast as `{success}` not `{flag}` (CR-3 regression guard).
- map.spec.ts asserts uploadSpotPhoto forwards `onUploadProgress` config (PATTERNS risk-flag #7 regression guard).
- mapStore.spec.ts asserts saveSpot re-fetches list and deleteSpot local-mutates (mirror fuelStore).
- Map.spec.ts stubs window.AMapLoader/AMap/navigator.geolocation/URL.createObjectURL and asserts FAB→geo, marker→selectedSpot, nav→androidamap://.
- Map.spec.ts asserts CR-2 call order: `convertFrom` called BEFORE `reverseGeocode` in the capture flow (T-13-FE-05 regression guard — `toHaveBeenCalledAfter`).
- Map.spec.ts has the D-22 skip-list with 7 `vi.skipIf` tests + top comment referencing 13-VALIDATION Manual-Only rows.
- vitest.config.ts unchanged (jsdom stays — CR-4).
- Full `pnpm test:unit` suite still green (no regression to existing tests).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→App backend over HTTPS | All `/map/*` calls cross here; JWT required (backend shipped in 13-02 — no whitelist entries) |
| Browser→Gaode JS API CDN | AMapLoader.js + tile requests; JS API key is public/domain-restricted (Pitfall 4); security code is browser-side verification |
| Browser Geolocation API→OS | navigator.geolocation.getCurrentPosition crosses OS permission gate; HTTPS required in production (Pitfall 3) |
| App backend→Gaode Web Service (server-side) | Reverse geocode backend → restapi.amap.com with `GAODE_WEB_API_KEY`; key never crosses to browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-FE-01 | Spoofing | Map.vue capture flow accepting manually-entered name | low | accept | Fishing spots don't need identity-verified location; name is user-owned, displayed via Vue `{{ name }}` auto-escape (no v-html) — XSS-covered by Vue default escaping |
| T-13-FE-02 | Tampering | Upload photo path traversal / mime spoof in `uploadSpotPhoto` | medium | mitigate | Reuse existing `/file/upload/raw/v1` (file.proto) — backend already handles filename/mime; do NOT add any browser-side path normalization. Map.spec.ts asserts only that formData carries the File; server is the trust boundary |
| T-13-FE-03 | Information Disclosure | Gaode JS API key + security code exposed to browser | low | accept | Key is DESIGNED to be public (domain-restricted in Gaode console — Pitfall 4). Security code is browser-side verification, NOT a secret. The server-side reverse-geocode key (referenced by env var `GAODE_WEB_API_KEY` in the pinned backend) MUST NEVER appear in frontend code — verified by a grep asserting zero occurrences under price_recorder_vue/src/ (Task 6 verify). <!-- planner-discipline-allow: GAODE_WEB_API_KEY --> |
| T-13-FE-04 | Information Disclosure | Reverse geocode address reveals user location to Gaode backend | low | accept | Single-user app; addresses go through backend (CONTEXT specifics line 146 — reverse geocode is NEVER called directly from browser); Gaode already sees the regeo request, which is the documented Gaode Web Service usage |
| T-13-FE-05 | Tampering | WGS-84→GCJ-02 conversion skipped → 300-500m offset (Pitfall 1) | high | mitigate | EVERY marker creation path AND the reverseGeocode-display call MUST go through `convertWgs84ToGcj02` (CR-2). map.spec.ts asserts `convertFrom` is invoked; Map.vue onMounted marker loop + addSpotMarker + capture FAB all call it. Storing RAW WGS-84 in SpotEntity is intentional (RESEARCH Pattern 5 — source-truthful); GCJ-02 is display concern only |
| T-13-FE-06 | Spoofing | GetSpot path-template route 404 if frontend uses query form | medium | mitigate | CR-1: map.ts `getSpot` MUST build `/map/get/${encodeURIComponent(id)}` (matches Kratos register `/map/get/{id}`). map.spec.ts asserts `getMock` called with `/map/get/5` — regression guard against PATTERNS.md risk-flag #2 misguidance |
| T-13-FE-07 | Denial of Service | Geolocation API blocked on HTTP in production → capture FAB silently fails (Pitfall 3) | medium | mitigate | `.env.example` documents HTTPS requirement; Map.vue capture error handler `.toast.error('无法获取位置')` surfaces failure; D-22 skip-list reroutes the real denial flow to MANUAL UAT in 13-VALIDATION.md (Geolocation-denied fallback row). Localhost whitelisted automatically |
| T-13-FE-SC | Tampering | pnpm install (no new packages in 13-03) | low | accept | No new npm packages installed this phase (RESEARCH Package Legitimacy Audit — Gaode loads via CDN, no registry packages). AMapLoader.js is loaded from `webapi.amap.com` (official Gaode CDN) — domain-pinned in index.html; not a package registry concern |

</threat_model>

<verification>
## Plan-Level Verification (closes the 4 FAILED truths in 13-VERIFICATION.md)

After all 6 tasks ship:

1. **Traceability hop closed** — `grep 'MAP-0[123]' .planning/REQUIREMENTS.md` returns ≥3 matches; `grep 'MAP-' Traceability` returns 3 rows pointing to Phase 13. Task 1.

2. **SC-1 capture → save (VERIFICATION gap #1)** — Map.vue capture FAB → getCurrentPosition → convertFrom → reverseGeocode (GCJ-02) → bottom-sheet → name-only submit → saveSpot posts serialized SpotEntity (RAW WGS-84 lat/lng as strings) to `/map/save/v1` → toast → marker added at GCJ-02 position. Verified by map.spec.ts (payload serialization) + Map.spec.ts (FAB→getCurrentPosition stub + address chip render) + manual UAT (real GPS field test, rerouted to 13-VALIDATION).

3. **SC-2 view/edit/delete (VERIFICATION gap #2)** — onMounted fetchSpots → markers for all spots; marker click → setSelectedSpot → spot-bottom-sheet with Edit/Delete/Navigate; Delete → confirm → mapApi.deleteSpot returns `{success:true}` (CR-3) → marker removed + local-mutate. Verified by mapStore.spec.ts (delete local-mutate) + map.spec.ts (deleteSpot returns success not flag) + Map.spec.ts (marker click → setSelectedSpot stub).

4. **SC-3 view + navigate deep-link (VERIFICATION gap #3)** — full-screen AMap.Map renders (manual); each saved spot has a marker; Navigate button → `window.location.href` set to `androidamap://navi?...&dev=0` (or `iosamap://` on iOS UA) with 2s fallback to `https://uri.amap.com/navigation`. Verified by Map.spec.ts (nav button sets location.href starting with `androidamap://navi` and `dev=0`) + manual UAT (Gaode App actually opens, rerouted to 13-VALIDATION).

5. **SC-4 persistence + mobile (VERIFICATION gap #4)** — saveSpot posts serialized SpotEntity → backend GORM persists (shipped 13-01); listSpots on reload returns same spots. Vitest suite green (`pnpm test:unit`). Mobile touch gestures enumerated as 7 D-22 `vi.skipIf` tests + re-routed to 13-VALIDATION Manual-Only (Task 1 extended the table). HTTPS-for-Geolocation requirement documented in `.env.example`.

6. **CR-1/CR-2/CR-3 regression guards** — map.spec.ts explicitly asserts the three contract-fidelity resolutions (path-template / GCJ-02-preconvert / `{success}` reply). These guards prevent future copy-paste drift from fuel.ts.

## Sampling Rate (inherited from 13-VALIDATION.md)

- After Task 2/3/4/5/6 commits: `cd price_recorder_vue && pnpm tsc --noEmit && pnpm test:unit --run`
- After Task 6 final: full `pnpm test:unit` (no regression to existing DebtDetail/fuel specs).
- `go test ./blog/internal/...` NOT required — backend untouched.
</verification>

<success_criteria>
- [ ] `src/api/map.ts`, `src/stores/mapStore.ts`, `src/view/Map.vue` exist and compile (`pnpm tsc --noEmit` exit 0).
- [ ] Map.vue wires AMapLoader with `import.meta.env.VITE_GAODE_JS_API_KEY` + sets `window._AMapSecurityConfig` at module scope.
- [ ] Capture FAB → getCurrentPosition → convertFrom (WGS-84→GCJ-02) → reverseGeocode (GCJ-02 lng,lat per CR-2) → bottom-sheet → name-only submit → saveSpot (RAW WGS-84 lat/lng per CR-2) → toast → marker at GCJ-02.
- [ ] Spot bottom-sheet on marker click shows Edit/Delete/Navigate; Delete confirms → mapApi.deleteSpot returns `{success:true}` (CR-3) → marker removed + mapStore local-mutate.
- [ ] Navigate button → `window.location.href = androidamap://navi?...&dev=0` (or iosamap://) with uri.amap.com fallback.
- [ ] Filter FAB (top-right) opens slide-down overlay; chip/search hides non-matching markers WITHOUT auto-pan; separate "fit view" button calls setFitView (D-19).
- [ ] Empty-state tooltip when spots.length===0; geolocation fallback chain per D-20 (GPS → last-known spot → Guangzhou default GCJ-02).
- [ ] `src/__tests__/api/map.spec.ts`, `src/__tests__/stores/mapStore.spec.ts`, `src/__tests__/view/Map.spec.ts` exist; `pnpm test:unit --run` for these three PASSES; full suite still green.
- [ ] Map.spec.ts has all 7 D-22 `vi.skipIf` skipped tests with comment block + refs to 13-VALIDATION.md Manual-Only (reroute the live-map behaviors).
- [ ] `.env.example` has both VITE_ vars and Gaode console URL.
- [ ] AppLayout.vue navItems has the 6th `地图` entry (D-14); router/index.ts has `/map → Map.vue` route with requiresAuth:true.
- [ ] `.planning/REQUIREMENTS.md` Fishing Spot Map section registers MAP-01/02/03 + 3 Traceability rows.
- [ ] `.planning/phases/13-fishing-spot-map/13-VALIDATION.md` Manual-Only table extended with the 7 D-22 rerouted behaviors.
- [ ] NO files under `blog/` modified (backend PINNED).
- [ ] `request.ts` UNCHANGED (axios config forwarding verified, not modified).
- [ ] map.spec.ts regression guards pass: getSpot path-template (`/map/get/5`), deleteSpot `{success}` not `{flag}`, saveSpot payload lat/lng serialized as strings.
</success_criteria>

<output>
Create `.planning/phases/13-fishing-spot-map/13-03-SUMMARY.md` when done, mirroring 13-02-SUMMARY.md frontmatter (`phase: 13-fishing-spot-map`, `plan: 03`, `subsystem: frontend`, `requires: [06-frontend-backend-integration, 13-01, 13-02]`, `provides: [Vue map UI, map.ts, mapStore.ts, MAP req registration closed]`, `affects: []`, `requirements-completed: [MAP-01, MAP-02, MAP-03]`, `status: complete`). The SUMMARY must explicitly note the contract-fidelity resolutions (CR-1/CR-2/CR-3/CR-4) and which VERIFICATION gaps each task closed.
</output>
</content>
</invoke>