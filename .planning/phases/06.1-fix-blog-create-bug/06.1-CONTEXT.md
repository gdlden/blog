# Phase 6.1: Fix Blog Create Bug - Context

**Gathered:** 2026-04-05
**Status:** Ready for planning

<domain>
## Phase Boundary

Fix the bug where creating a new blog post shows "创建失败" (creation failed). This is a gap-closure phase to address issues discovered after Phase 6 completion.

Root causes identified:
1. Wire injection code (wire_gen.go) is outdated - missing new UpdatePost/DeletePost dependencies
2. Need to verify backend starts correctly and API endpoints work
3. Frontend error handling may need improvement for better diagnostics

</domain>

<decisions>
## Implementation Decisions

### Root Cause Fix
- **D-01:** Run `wire` command to regenerate `wire_gen.go` with new PostUsecase methods
- **D-02:** Verify backend builds and starts successfully after wire regeneration
- **D-03:** Test POST /post/add/v1 endpoint directly to confirm it works

### Verification Strategy
- **D-04:** Use curl or API test to verify backend endpoint before frontend testing
- **D-05:** Check browser network tab for exact error response if frontend still fails
- **D-06:** Add better error logging if needed for diagnostics

### Scope Limitation
- **D-07:** Focus only on fixing the create bug, not refactoring other code
- **D-08:** Do not add new features, only fix the existing broken flow

### Claude's Discretion
- Whether to add request/response logging for debugging
- Whether to improve frontend error messages
- Exact testing approach (curl vs UI testing)

</decisions>

<canonical_refs>
## Canonical References

### Backend
- `blog/cmd/blog/wire.go` - Wire injector definition
- `blog/cmd/blog/wire_gen.go` - Generated wire code (outdated)
- `blog/internal/biz/post.go` - PostUsecase with new UpdatePost/DeletePost methods
- `blog/internal/service/post.go` - PostService handlers

### Frontend
- `price_recorder_vue/src/api/blog.ts` - API client for blog
- `price_recorder_vue/src/stores/blogStore.ts` - Store with createPost action

### Related Plans
- `06-01-PLAN.md` - Original backend CRUD implementation
- `06-02-PLAN.md` - Original frontend blog implementation

</canonical_refs>

<code_context>
## Existing Code Insights

### The Problem
- Phase 6-01 added UpdatePost/DeletePost methods to PostUsecase
- `make api` regenerated protobuf code
- But `wire` was NOT run to update dependency injection
- `wire_gen.go` still has old provider set without new methods

### Current wire_gen.go
Created March 29, before Phase 6 changes. Needs regeneration.

### How to Fix
```bash
cd blog/cmd/blog
go run github.com/google/wire/cmd/wire@latest
# or just: wire
```

### Rebuild and Test
```bash
cd blog
make build
make run
# Test in another terminal:
curl -X POST http://localhost:8000/post/add/v1 \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"title":"Test","content":"Test content"}'
```

</code_context>

<specifics>
## Specific Ideas

- The error "创建失败" likely comes from blogStore.ts catching an error
- Check if it's network error (backend not running) or HTTP error (500 from backend)
- Backend startup error "undefined: wireApp" confirms wire_gen.go is broken

</specifics>

<deferred>
## Deferred Ideas

- Improving form validation - not in scope
- Adding request logging middleware - not in scope
- UI/UX improvements - not in scope
- Performance optimizations - not in scope

</deferred>

---

*Phase: 06.1-fix-blog-create-bug*
*Context gathered: 2026-04-05*
