---
phase: 06-frontend-backend-integration
plan: 04
type: execute
wave: 4
depends_on:
  - 06-02
  - 06-03
files_modified:
  - price_recorder_vue/package.json
  - price_recorder_vue/src/main.ts
  - price_recorder_vue/src/stores/blogStore.ts
  - price_recorder_vue/src/stores/debtStore.ts
  - price_recorder_vue/src/view/BlogList.vue
  - price_recorder_vue/src/view/DebtList.vue
autonomous: true
requirements:
  - BLOG-06
  - DEBT-06
  - INT-01
  - INT-02
must_haves:
  truths:
    - Toast notifications display for all CRUD operations
    - Success toasts show for create, update, delete operations
    - Error toasts show when API operations fail
    - Toast auto-dismisses after 3-5 seconds
    - Blog and Debt stores integrate toast notifications
    - Frontend builds without errors
  artifacts:
    - path: price_recorder_vue/package.json
      provides: vue-toastification dependency
      contains: "vue-toastification"
    - path: price_recorder_vue/src/main.ts
      provides: Toast plugin configuration
      contains: "vue-toastification"
    - path: price_recorder_vue/src/stores/blogStore.ts
      provides: Blog store with toast integration
      contains: "useToast"
    - path: price_recorder_vue/src/stores/debtStore.ts
      provides: Debt store with toast integration
      contains: "useToast"
  key_links:
    - from: price_recorder_vue/src/stores/blogStore.ts
      to: vue-toastification
      via: useToast() composable
      pattern: "const toast = useToast()"
    - from: price_recorder_vue/src/stores/debtStore.ts
      to: vue-toastification
      via: useToast() composable
      pattern: "const toast = useToast()"
---

<objective>
Integrate toast notifications for user feedback across all CRUD operations. Install and configure vue-toastification, then update stores to show success and error messages.

Purpose: Provide clear user feedback for all operations and complete the frontend integration phase.
Output: Working toast notifications for blog and debt CRUD operations, with proper error handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-frontend-backend-integration/06-CONTEXT.md
@.planning/phases/06-frontend-backend-integration/06-RESEARCH.md
@price_recorder_vue/package.json
@price_recorder_vue/src/main.ts
@price_recorder_vue/src/stores/blogStore.ts
@price_recorder_vue/src/stores/debtStore.ts

<interfaces>
From price_recorder_vue/src/main.ts (current):
```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import router from './router'
import './index.css'

const app = createApp(App)
app.use(createPinia())
app.use(router)
app.mount('#app')
```

From price_recorder_vue/src/stores/blogStore.ts (from Plan 02):
```typescript
import { ref, computed } from 'vue'
import { defineStore } from 'pinia'
import * as blogApi from '@/api/blog'

export const useBlogStore = defineStore('blog', () => {
  // State, getters, actions without toast
})
```

Toast pattern from RESEARCH.md:
```typescript
import { useToast } from 'vue-toastification'
const toast = useToast()

// Success
toast.success('博文创建成功')

// Error
toast.error(err.message || '创建失败')
```

Main.ts toast configuration:
```typescript
import Toast from 'vue-toastification'
import 'vue-toastification/dist/index.css'

app.use(Toast, {
  position: 'top-right',
  timeout: 3000,
  closeOnClick: true,
  pauseOnFocusLoss: true,
  pauseOnHover: true,
  draggable: true,
  draggablePercent: 0.6,
  showCloseButtonOnHover: false,
  hideProgressBar: false,
  closeButton: 'button',
  icon: true,
  rtl: false,
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure vue-toastification</name>
  <read_first>
    - price_recorder_vue/package.json (current dependencies)
    - price_recorder_vue/src/main.ts (current setup)
  </read_first>
  <files>price_recorder_vue/package.json, price_recorder_vue/src/main.ts</files>
  <action>
    Install vue-toastification and configure it in main.ts.

    1. Install the package:
       ```bash
       cd /Users/hukss/dev/blog/price_recorder_vue
       pnpm add vue-toastification@next
       ```

    2. Update price_recorder_vue/src/main.ts:
       - Add import: `import Toast from 'vue-toastification'`
       - Add import: `import 'vue-toastification/dist/index.css'`
       - Add app.use(Toast, { ... }) before app.mount('#app')

    Toast configuration (per D-17 through D-19):
    ```typescript
    app.use(Toast, {
      position: 'top-right',
      timeout: 3000,
      closeOnClick: true,
      pauseOnFocusLoss: true,
      pauseOnHover: true,
      draggable: true,
      draggablePercent: 0.6,
      showCloseButtonOnHover: false,
      hideProgressBar: false,
      closeButton: 'button',
      icon: true,
      rtl: false,
    })
    ```

    Per D-17 through D-19: Toast notifications for success/error feedback, auto-dismiss 3-5s.
  </action>
  <acceptance_criteria>
    - price_recorder_vue/package.json contains "vue-toastification" in dependencies
    - price_recorder_vue/src/main.ts imports Toast from 'vue-toastification'
    - price_recorder_vue/src/main.ts imports 'vue-toastification/dist/index.css'
    - price_recorder_vue/src/main.ts calls app.use(Toast, { ... })
    - Toast configuration has position: 'top-right' and timeout: 3000
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/hukss/dev/blog/price_recorder_vue && grep -q "vue-toastification" package.json && grep -q "vue-toastification" src/main.ts && echo "Toast installed and configured"</automated>
  </verify>
  <done>vue-toastification installed and configured in main.ts</done>
</task>

<task type="auto">
  <name>Task 2: Add toast notifications to blog store</name>
  <read_first>
    - price_recorder_vue/src/stores/blogStore.ts (current implementation from Plan 02)
  </read_first>
  <files>price_recorder_vue/src/stores/blogStore.ts</files>
  <action>
    Integrate toast notifications into the blog store actions.

    1. Add import: `import { useToast } from 'vue-toastification'`

    2. Inside the store function, add: `const toast = useToast()`

    3. Update fetchPosts action:
       - On error: `toast.error(error.value)` (error already set)

    4. Update createPost action:
       - On success: `toast.success('博文创建成功')`
       - On error: `toast.error(err.message || '创建失败')`

    5. Update updatePost action:
       - On success: `toast.success('博文更新成功')`
       - On error: `toast.error(err.message || '更新失败')`

    6. Update deletePost action:
       - On success: `toast.success('博文删除成功')`
       - On error: `toast.error(err.message || '删除失败')`

    Per D-17 through D-19: Toast shows success/error feedback for all operations.
  </action>
  <acceptance_criteria>
    - price_recorder_vue/src/stores/blogStore.ts imports useToast from 'vue-toastification'
    - Store creates toast instance with useToast()
    - createPost action calls toast.success('博文创建成功') on success
    - createPost action calls toast.error() on error
    - updatePost action calls toast.success('博文更新成功') on success
    - updatePost action calls toast.error() on error
    - deletePost action calls toast.success('博文删除成功') on success
    - deletePost action calls toast.error() on error
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useToast" price_recorder_vue/src/stores/blogStore.ts && grep -q "toast.success" price_recorder_vue/src/stores/blogStore.ts && grep -q "博文创建成功" price_recorder_vue/src/stores/blogStore.ts && echo "Blog store has toast"</automated>
  </verify>
  <done>Blog store integrated with toast notifications</done>
</task>

<task type="auto">
  <name>Task 3: Add toast notifications to debt store</name>
  <read_first>
    - price_recorder_vue/src/stores/debtStore.ts (current implementation from Plan 03)
  </read_first>
  <files>price_recorder_vue/src/stores/debtStore.ts</files>
  <action>
    Integrate toast notifications into the debt store actions.

    1. Add import: `import { useToast } from 'vue-toastification'`

    2. Inside the store function, add: `const toast = useToast()`

    3. Update fetchDebts action:
       - On error: `toast.error(error.value)` (error already set)

    4. Update createDebt action:
       - On success: `toast.success('债务记录创建成功')`
       - On error: `toast.error(err.message || '创建失败')`

    5. Update updateDebt action:
       - On success: `toast.success('债务记录更新成功')`
       - On error: `toast.error(err.message || '更新失败')`

    6. Update deleteDebt action:
       - On success: `toast.success('债务记录删除成功')`
       - On error: `toast.error(err.message || '删除失败')`

    Per D-17 through D-19: Toast shows success/error feedback for all operations.
  </action>
  <acceptance_criteria>
    - price_recorder_vue/src/stores/debtStore.ts imports useToast from 'vue-toastification'
    - Store creates toast instance with useToast()
    - createDebt action calls toast.success('债务记录创建成功') on success
    - createDebt action calls toast.error() on error
    - updateDebt action calls toast.success('债务记录更新成功') on success
    - updateDebt action calls toast.error() on error
    - deleteDebt action calls toast.success('债务记录删除成功') on success
    - deleteDebt action calls toast.error() on error
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useToast" price_recorder_vue/src/stores/debtStore.ts && grep -q "toast.success" price_recorder_vue/src/stores/debtStore.ts && grep -q "债务记录创建成功" price_recorder_vue/src/stores/debtStore.ts && echo "Debt store has toast"</automated>
  </verify>
  <done>Debt store integrated with toast notifications</done>
</task>

<task type="auto">
  <name>Task 4: Verify frontend build and integration</name>
  <read_first>
    - price_recorder_vue/package.json
  </read_first>
  <files></files>
  <action>
    Verify the frontend builds successfully with all integrations.

    1. Run type check:
       ```bash
       cd /Users/hukss/dev/blog/price_recorder_vue
       pnpm type-check
       ```

    2. Run build:
       ```bash
       pnpm build
       ```

    3. Run lint (optional but recommended):
       ```bash
       pnpm lint
       ```

    Expected outcomes:
    - type-check passes without errors
    - build completes successfully
    - No TypeScript errors related to vue-toastification
    - No missing import errors

    If there are type errors with vue-toastification, install types:
    ```bash
    pnpm add -D @types/vue-toastification
    ```
    Or add a type declaration in shims-vue.d.ts if needed.
  </action>
  <acceptance_criteria>
    - pnpm type-check completes without errors
    - pnpm build completes without errors
    - No TypeScript errors in src/stores/blogStore.ts
    - No TypeScript errors in src/stores/debtStore.ts
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/hukss/dev/blog/price_recorder_vue && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <done>Frontend builds successfully with toast integration</done>
</task>

</tasks>

<verification>
1. vue-toastification package installed
2. Toast plugin configured in main.ts with proper options
3. Blog store shows toast on create, update, delete
4. Debt store shows toast on create, update, delete
5. Frontend builds without TypeScript errors
6. Toast messages are in Chinese as per UI context
</verification>

<success_criteria>
- Toast notifications appear for all CRUD operations
- Success messages: "博文创建成功", "博文更新成功", "博文删除成功", "债务记录创建成功", etc.
- Error messages display when API calls fail
- Toast auto-dismisses after 3 seconds
- Frontend builds and type-checks successfully
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-backend-integration/06-04-SUMMARY.md`
</output>
