---
phase: 05-database-config-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - blog/cmd/blog/main.go
  - blog/.env.example
autonomous: true
requirements:
  - CFG-01
must_haves:
  truths:
    - Database connection source can be configured via environment variable DATA_DATABASE_SOURCE
    - Environment variable takes precedence over YAML config when both are present
    - Existing config.yaml behavior remains backward compatible when env var is not set
    - Clear documentation exists showing supported environment variables and format
  artifacts:
    - path: blog/cmd/blog/main.go
      provides: Kratos configuration with env source
      contains: env.NewSource("DATA_DATABASE")
    - path: blog/.env.example
      provides: Environment variable documentation
      contains: DATA_DATABASE_SOURCE
  key_links:
    - from: blog/cmd/blog/main.go
      to: conf.Data.Database.Source
      via: config.Scan()
      pattern: env.NewSource.*DATA_DATABASE
---

<objective>
Enable database connection source to be injected via environment variables for flexible deployment and configuration management.

Purpose: Support cloud deployment scenarios (Railway, Render, AWS RDS, etc.) where database credentials are provided as environment variables rather than static config files.
Output: Enhanced main.go with env source support, .env.example documentation, and verified backward compatibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-database-config-enhancement/05-CONTEXT.md
@.planning/phases/05-database-config-enhancement/05-RESEARCH.md

<!-- Current implementation files -->
@blog/cmd/blog/main.go
@blog/internal/conf/conf.proto
@blog/configs/config.yaml
@blog/internal/data/data.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Kratos env source to main.go configuration</name>
  <files>blog/cmd/blog/main.go</files>
  <read_first>
    - blog/cmd/blog/main.go (current state: single file source)
    - blog/internal/conf/conf.proto (protobuf schema: Data.Database.Source field)
    - blog/configs/config.yaml (current database source format)
  </read_first>
  <action>
    Modify blog/cmd/blog/main.go to add environment variable configuration source:

    1. Add import: "github.com/go-kratos/kratos/v2/config/env"

    2. Update the config.New() call to include env source AFTER file source:

    FROM:
    ```go
    c := config.New(
        config.WithSource(
            file.NewSource(flagconf),
        ),
    )
    ```

    TO:
    ```go
    c := config.New(
        config.WithSource(
            file.NewSource(flagconf),          // Base config from YAML
            env.NewSource("DATA_DATABASE"),    // Override with env vars
        ),
    )
    ```

    This leverages Kratos merge behavior where later sources override earlier ones (per D-03, D-07, D-08).
    The prefix "DATA_DATABASE" maps env var DATA_DATABASE_SOURCE to conf.Data.Database.Source.
  </action>
  <verify>
    <automated>cd /Users/hukss/dev/blog/blog && go build ./cmd/blog/...</automated>
  </verify>
  <acceptance_criteria>
    - blog/cmd/blog/main.go contains import: "github.com/go-kratos/kratos/v2/config/env"
    - blog/cmd/blog/main.go contains env.NewSource("DATA_DATABASE") in config.WithSource()
    - env.NewSource appears AFTER file.NewSource in the source list
    - go build ./cmd/blog/... exits with code 0
  </acceptance_criteria>
  <done>
    main.go compiles successfully with env source added, file source first, env source second
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example documentation file</name>
  <files>blog/.env.example</files>
  <read_first>
    - blog/configs/config.yaml (reference for current database source format)
    - blog/internal/conf/conf.proto (protobuf schema for field mapping)
  </read_first>
  <action>
    Create blog/.env.example with documentation for environment variable configuration:

    ```
    # Blog Service Environment Variables
    # Copy this file to .env and modify values for your deployment

    # Database Configuration
    # Full DSN format (overrides config.yaml when set)
    # Format: host=HOST user=USER password=PASSWORD dbname=DB port=PORT sslmode=MODE
    DATA_DATABASE_SOURCE="host=localhost user=postgres password=postgres dbname=postgres port=5432 sslmode=disable TimeZone=Asia/Shanghai"

    # Alternative for cloud providers using postgres:// URL format:
    # Note: GORM postgres driver accepts DSN format directly. URL format may require conversion.
    # DATA_DATABASE_SOURCE="postgres://user:password@host:port/dbname?sslmode=require"

    # How it works:
    # - DATA_DATABASE_SOURCE maps to conf.Data.Database.Source via Kratos env source
    # - Environment variables take precedence over config.yaml values
    # - If DATA_DATABASE_SOURCE is not set, falls back to config.yaml
    ```

    Per D-04/D-05: Primary variable is DATABASE_URL format, but Kratos mapping requires DATA_DATABASE_SOURCE.
    Per D-09: Document backward compatibility behavior.
  </action>
  <verify>
    <automated>test -f /Users/hukss/dev/blog/blog/.env.example && echo "File exists"</automated>
  </verify>
  <acceptance_criteria>
    - blog/.env.example exists
    - blog/.env.example contains "DATA_DATABASE_SOURCE="
    - blog/.env.example contains documentation about DSN format
    - blog/.env.example documents that env vars take precedence over config.yaml
    - blog/.env.example documents backward compatibility (falls back to config.yaml)
  </acceptance_criteria>
  <done>
    .env.example file created with complete documentation for DATA_DATABASE_SOURCE usage
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify backward compatibility and env override behavior</name>
  <files></files>
  <read_first>
    - blog/cmd/blog/main.go (verify final state)
    - blog/configs/config.yaml (verify fallback config exists)
  </read_first>
  <action>
    Verify the implementation meets all success criteria:

    1. Test backward compatibility (no env var set):
       ```bash
       cd /Users/hukss/dev/blog/blog
       unset DATA_DATABASE_SOURCE
       go build ./cmd/blog/...
       # Should compile and use config.yaml values
       ```

    2. Verify env var is recognized (compile-time check):
       The code now imports env package and creates env source.
       Runtime verification would require actual database connection.

    3. Run existing tests to ensure no regression:
       ```bash
       cd /Users/hukss/dev/blog/blog
       go test ./internal/data/... -v
       ```

    4. Document verification in task output.
  </action>
  <verify>
    <automated>cd /Users/hukss/dev/blog/blog && go test ./internal/data/... -v</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/data/... passes with no failures
    - go build ./cmd/blog/... succeeds
    - blog/configs/config.yaml still exists unchanged (backward compatibility preserved)
  </acceptance_criteria>
  <done>
    Backward compatibility verified (existing tests pass, config.yaml unchanged), env source properly integrated
  </done>
</task>

</tasks>

<verification>
Overall phase verification:
1. Build succeeds: cd blog && go build ./cmd/blog/...
2. Tests pass: cd blog && go test ./internal/data/... -v
3. Env source is configured: grep -q 'env.NewSource("DATA_DATABASE")' blog/cmd/blog/main.go
4. Documentation exists: test -f blog/.env.example
</verification>

<success_criteria>
- Database source can be configured via DATA_DATABASE_SOURCE environment variable
- Environment variable takes precedence over YAML config (env source loaded after file source)
- Existing config.yaml behavior remains backward compatible (works when env var not set)
- Clear documentation exists in .env.example showing supported variables and format
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-database-config-enhancement/05-01-SUMMARY.md`
</output>
